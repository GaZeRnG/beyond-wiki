[build]
    command = "next build"
    publish = ".next"

[[plugins]]
    package = "@netlify/plugin-nextjs"

[build.environment]
    SECRETS_SCAN_OMIT_PATHS = ".netlify/**,.next/**,package-lock.json"

[[redirects]]
    from = "/"
    to = "/"
    status = 200
    force = true
